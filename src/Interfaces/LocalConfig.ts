export namespace LocalConfig {
    export interface blockchain {
        apiKey: string;
        walletUrl: string;
    }
    export interface wallet {
        guid: string;
        password: string;
    }
}